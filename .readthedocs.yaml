version: 2

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.11"
  apt_packages:
    - graphviz
    - rsync
    - unzip

  commands:
    - pip install docs/requirements.txt
    - unzip mva_docs.zip -d $READTHEDOCS_OUTPUT/html
